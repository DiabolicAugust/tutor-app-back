node_modules

# Keep environment variables out of version control
.env
.env.local

# Compiled output
/dist
*.tsbuildinfo

# Generated Prisma client — run `npm run db:generate` after cloning
/generated/prisma

# Uploaded files. The record of truth is the database; these are the bytes.
/uploads

# Test artefacts
/coverage
